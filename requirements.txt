#  Core deep learning 
# Install the torch build that matches your CUDA version:
#   https://pytorch.org/get-started/locally/
torch>=2.1,<3.0
torchvision>=0.16,<1.0
timm>=0.9.12                # pretrained model hub (DenseNet-121, ViT-B/16, …)
peft>=0.7.0                 # LoRA adapters for ViT (parameter-efficient fine-tuning)

#  Data / augmentation 
albumentations>=1.4.0       # aerial-aware augmentation pipeline
opencv-python>=4.8          # image I/O, Grad-CAM overlay, resize
Pillow>=10.0                # TIFF / PNG loading
numpy>=1.24,<3.0
pandas>=2.0
scipy>=1.10                 # required by scikit-learn & umap-learn

#  ML utilities 
scikit-learn>=1.3           # metrics, train/val/test split, t-SNE, IsolationForest
optuna>=3.5                 # Bayesian hyper-parameter search (TPE + Median pruner)

#  Visualisation 
matplotlib>=3.7
seaborn>=0.13

#  Reporting / dashboard 
markdown>=3.5               # Markdown → HTML conversion for the final report
tabulate>=0.9               # DataFrame.to_markdown() support
streamlit>=1.30             # inference dashboard (dashboard/app.py)
tensorboard>=2.15           # training-curve visualisation

#  Config / utilities 
pyyaml>=6.0                 # config.yaml loading
tqdm>=4.66                  # progress bars

#  Notebook 
jupyter>=1.0
ipykernel>=6.25

#  Optional extras 
# Install these for richer outputs; the pipeline degrades gracefully if absent.
umap-learn>=0.5.7           # UMAP embedding plots in EDA (optional); 0.5.7+ avoids numba==0.53.1 pin
fvcore>=0.1.5               # FLOPs counting — falls back to thop, then skips (optional)
thop>=0.1.1                 # FLOPs counting fallback (optional)
weasyprint>=60              # PDF report export (optional)
